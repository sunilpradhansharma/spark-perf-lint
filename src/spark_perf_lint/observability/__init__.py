"""Observability package for spark-perf-lint.

Provides a provider-agnostic tracing interface for recording analysis
runs, rule firings, and LLM calls. Ships with a JSON file-based tracer
by default; LangSmith integration is available as an optional backend.
"""
